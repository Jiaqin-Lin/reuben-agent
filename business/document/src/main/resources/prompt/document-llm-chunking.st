你是一个文档分块专家。以下是一段文本的句子列表，每个句子前有 `[索引]` 标记。

请分析相邻句子之间的语义连贯性，找到**语义主题发生变化的断点**（即在此处应该切分为新段落）。

## 切分原则
- 当两个相邻句子讨论不同主题/话题时，在前一个句子后切分
- 段落主题转换处应该切分
- 步骤/操作序列的每一步之间可以切分
- 不要切分过细：如果相邻句子紧密相关（共享同一主语、承接关系明确），保持连续

## 输出格式
仅返回 JSON 数组，包含断点索引（即最后一个保持连续的句子索引）。
例如 [3, 7, 12] 表示在第3句后、第7句后、第12句后切分。

句子总数：<sentenceCount>

<sentences>
